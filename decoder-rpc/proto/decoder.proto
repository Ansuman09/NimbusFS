syntax = "proto3";

package decoder;

option go_package = "test-rpc/proto;proto";

service Decoder {
  rpc Decode (FileRequest) returns (DecodeResponse);
}

message FileRequest {
  string filename = 1;
}

message DecodeResponse {
  bool success = 1;
  string message = 2;
}
